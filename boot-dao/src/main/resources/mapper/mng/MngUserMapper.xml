<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.boot.dao.mng.MngUserMapper">
<select id="userList" resultType="com.boot.domain.mng.MngUser">
    select
    id,
    user_id as userId,
    status as status
    from mng_user
</select>

</mapper>
